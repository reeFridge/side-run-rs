[package]
name = "side-run"
version = "0.1.0"
authors = ["reeFridge <reefridgerator@gmail.com>"]

[dependencies]
byteorder = "1"
piston_window = "0.71"
conrod = { version = "0.55", features = ["piston"] }
